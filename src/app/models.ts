// Create model for an inventory item
export interface Inventory {
    name:string
    imageUrl:string
}

// Create model for an item customer selected
export interface CustomerSelection {
    name:string
    quantity:number
}